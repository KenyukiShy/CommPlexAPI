# CommPlexAPI/requirements.txt
# Domain: The Mouth -- FastAPI Gateway dependencies
# Install: pip install -r requirements.txt --break-system-packages

# ── FastAPI stack ─────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9       # Required for form data / file uploads

# ── Database ──────────────────────────────────────────────
sqlalchemy>=2.0.28
alembic>=1.13.1               # DB migrations (future-proofing)
# Uncomment for PostgreSQL:
# psycopg2-binary>=2.9.9

# ── HTTP client (webhook simulation, ntfy alerts) ─────────
requests>=2.31.0
httpx>=0.27.0                 # Async HTTP for FastAPI test client

# ── Config / Environment ──────────────────────────────────
python-dotenv>=1.0.0

# ── Security ──────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT (for future auth layer)
passlib[bcrypt]>=1.7.4

# ── CORS ─────────────────────────────────────────────────
# Included in FastAPI -- no extra install needed

# ── Testing ───────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
